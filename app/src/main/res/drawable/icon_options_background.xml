<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded panel behind the per-icon options menu. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/icon_options_background" />
    <corners android:radius="@dimen/icon_options_corner_radius" />
    <stroke
        android:width="1dp"
        android:color="@color/icon_options_stroke" />
</shape>
